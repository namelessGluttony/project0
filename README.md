# project0

Starter repository for adding multiple files, folders, and experiments.

## Quick start

- Add your files and directories here.
- Check changes with `git status`.
- Commit when ready:

```bash
git add .
git commit -m "Initial commit"
```
